{
	"scripts": {
		"build": "cd backend && npm i --force && cd ../client && npm install --legacy-peer-deps && npm i --force && npm run build --legacy-peer-deps",
		"start": "node backend/server.js"
	},
	"dependencies": {
		"chart.js": "^4.4.2",
		"headlessui": "^0.0.0",
		"heroicons": "^2.1.1",
		"react-chartjs-2": "^5.2.0",
		"react-icons": "^5.0.1"
	}
}
